# Base Robot dependencies for every connector e2e suite.
robotframework~=7.0.0
paho-mqtt~=2.1
# DeviceLibrary owns the stack lifecycle: each Setup starts the protocol's docker compose
# file as its own project (named after a randomly generated serial), so suites are isolated
# and can run in parallel. The [docker] extra pulls device-test-core's compose adapter.
robotframework-devicelibrary[docker] @ git+https://github.com/thin-edge/robotframework-devicelibrary.git@1.26.1
